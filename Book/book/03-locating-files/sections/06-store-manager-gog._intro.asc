GOG Galaxy serves as the store manager for all GOG games.
The GOG Store Manager pinpoints the 'galaxy-2.0.db' file nested within the store managers directory.
Through decoding with Sqlite3, the 'SELECT productId, installationPath FROM InstalledBaseProducts' query unveils the location of the installed games.