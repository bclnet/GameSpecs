<UserControl x:Class="GameX.Cig.Apps.StarWords.Views.DocumentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="500">
    <Grid>
        <DataGrid x:Name="DataGrid" AutoGenerateColumns="False">
            <DataGrid.Columns >
                <DataGridTextColumn Header="LocId" Binding="{Binding Path=LocId}" MinWidth="50" />
                <DataGridTextColumn Header="English Text" Binding="{Binding Path=English}" MinWidth="50" />
                <DataGridTextColumn Header="Status" Binding="{Binding Path=Status}" MinWidth="50" />
                <DataGridTextColumn Header="Label" Binding="{Binding Path=Label}" MinWidth="50" />
                <DataGridTextColumn Header="Use" Binding="{Binding Path=Use}" MinWidth="50" />
                <DataGridTextColumn MinWidth="50" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
